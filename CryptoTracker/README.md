# CryptoTracker

A small Spring Boot application that tracks cryptocurrencies.

## Build

Requires Java and Maven. From project root:

```
mvn clean package
```

## Run

```
mvn spring-boot:run
```

## Notes

- Ensure you do not commit secret keys or passwords; use environment variables for sensitive configuration.
- This repo was prepared for GitHub push.